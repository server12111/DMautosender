version: '3.8'

services:
  bot:
    build: .
    restart: always
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/bot.db
      SESSIONS_PATH: /app/data/sessions
      LOGS_PATH: /app/data/logs
    volumes:
      - ./data:/app/data
